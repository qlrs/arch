alt + Return
    $TERMINAL
alt + d
    dmenu_run -fn "Liberation Mono-13" -nb "#000000" -nf "#ffffff" -sb "#0066ff"
alt + shift + e
    memeji.sh
alt + z
    gnome-calculator
alt + e
    $TERMINAL -e aerc
alt + w 
    $BROWSER
alt + shift + m 
    $TERMINAL -e ncmpcpp
alt + y 
    clipmenu
alt + b
    pcmanfm
alt + g
    steam
alt + shift + n
    $TERMINAL -e newsboat
alt + p
    $TERMINAL -e pulsemixer
alt + n
    $TERMINAL -e ranger
alt + equal
    pamixer --allow-boost -i 5
alt + minus
    pamixer --allow-boost -d 5
alt + shift + p
    mpc toggle
alt + m
    pamixer -t
alt + shift + period
    mpc next
alt + shift + comma
    mpc prev
alt + u
    dmenumount.sh
alt + r
    redditmenu.sh
