alt + Return
    $TERMINAL
alt + d
    dmenu_run
alt + shift + semicolon
    memeji.sh
alt + z
    gnome-calculator
alt + semicolon
    $TERMINAL -e nmtui
alt + e
    $TERMINAL -e ranger
alt + w 
    $BROWSER
alt + shift + m 
    $TERMINAL -e ncmpcpp
alt + y 
    clipmenu
alt + b
    pcmanfm
alt + shift + n
    $TERMINAL -e newsboat
alt + p
    $TERMINAL -e pulsemixer
alt + n
    $TERMINAL -e aerc
alt + equal
    pamixer --allow-boost -i 5
alt + minus
    pamixer --allow-boost -d 5
alt + shift + p
    mpc toggle
alt + m
    pamixer -t
alt + shift + backslash
    mpc next
alt + shift + bracketright
    mpc prev
alt + u
    dmenumount.sh
alt + r
    redditdmenu.sh
