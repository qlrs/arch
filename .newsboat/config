auto-reload yes

bind-key j down
bind-key k up
bind-key j next articlelist
bind-key k prev articlelist
bind-key J next-feed articlelist
bind-key K prev-feed articlelist
bind-key G end
bind-key g home
bind-key l open
bind-key h quit

browser firefox
player mpv
#need to press , before o
macro o set browser mpv ; open-in-browser ; toggle-article-read "read" ; set browser firefox

#color listfocus white magenta
#color info white black
#maybe delete these
#color listfocus_unread white magenta
#color listnormal_unread white magenta
